var newLetter = prompt("Enter a letter!");

// The `i` variable is a counter, to count how many times the loop has occurred
var i;

// Let’s create a loop that repeats 10 times
for (i = 0; i < 10; i++) {
  // The code in here will be executed 10 times
  document.write(newLetter + "<br>");
}
